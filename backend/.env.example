CARBON_DATABASE_DSN=postgresql+psycopg://carbon@127.0.0.1:5433/carbon
CARBON_VAULT_ROOT=~/.my-links/logs-obsidian/carbon/Notifications
CARBON_BIND_HOST=127.0.0.1
CARBON_BIND_PORT=8000
CARBON_LOG_LEVEL=INFO
# Trusted webview origins. After removing token auth, CORS origin checking is the
# drive-by defense, so override only to add custom dev origins.
# CARBON_CORS_ORIGINS=["http://localhost:5173","tauri://localhost","https://tauri.localhost"]
