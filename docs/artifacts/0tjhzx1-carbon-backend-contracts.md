# Slice 0: контракты Carbon backend

Slice 0 зафиксирован в следующих документах:

- архитектурный контракт: `docs/architecture/2026-08-09-carbon-backend-contracts.md`;
- module contract: `backend/MODULE_CONTRACT.md`;
- утверждённые слайды: `docs/artifacts/0tjhykc-0tjhvkr-slices.md`.

Зафиксированы API-модели и error envelope, frontmatter schema, идентификаторы, PATCH scope, локальные API-токены и scopes, file/DB consistency order, компенсации, rebuild invariants, opaque cursor pagination и SSE reconnect/full-refetch strategy.

Реализация backend-кода, миграции и endpoints в Slice 0 не выполнялись.

